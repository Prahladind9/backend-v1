package edu.prahlad.java.course2.oop.designPrinciples.encapsulateWhatVaries.OnClassLevel;

/**
 * After: tax calculation is hidden from the order class
 */
public class TaxCalculator {

    /**
     * + getTaxRate(country, state, product)
     * - getUSTax(state)
     * - getEUTax(country)
     * - getChineseTax(product)
     */
}
